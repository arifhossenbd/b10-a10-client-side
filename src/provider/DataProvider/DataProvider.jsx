import { DataContext } from "./DataContext";

const DataProvider = () => {
    return (
        <DataContext.Provider>
            
        </DataContext.Provider>
    );
};

export default DataProvider;